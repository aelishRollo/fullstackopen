# Notes example

This is the Notes application at the end of Part 2c, ready for the examples in Part 2d.

Run the frontend and JSON Server in separate terminals:

```sh
npm run dev
npm run server
```

The frontend runs on Vite's displayed URL and reads notes from `http://localhost:3001/notes`.
