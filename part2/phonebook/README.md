# Phonebook

The Phonebook exercise application is ready to continue with exercise 2.12 in Part 2d.

Run the frontend and JSON Server in separate terminals:

```sh
npm run dev
npm run server
```

The frontend reads people from `http://localhost:3001/persons`.
